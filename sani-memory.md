2026-04-20: Created `sani.md` and `sani-memory.md` to satisfy workspace session requirements before starting code investigation.
2026-04-20: Added `game/render/mobileGestureMath.ts` and `game/mobilePlacement.ts`, updated `game/render/IsoCameraSystem.ts` and `engine/input/InputSystem.ts` to stabilize mobile zoom/rotation and stop multi-touch taps from triggering bogus placement clicks.
2026-04-20: Updated `game/AureusWorld.ts` and `App.tsx` so mobile build confirmation uses one world-owned placement path and only clears the pinned ghost after a successful placement.
2026-04-20: Added `tests/mobile-gestures.test.ts` and `tests/mobile-placement.test.ts`; verified with `node --test tests/mobile-gestures.test.ts tests/mobile-placement.test.ts` and `npm run build`.
2026-05-06: Added adaptive rounded-box support to `engine/render/utils/VoxelBuilder.ts` so larger building parts render with softened edges while tiny details stay plain boxes for performance.
2026-05-06: Added reusable `cylinder(...)` and `taperedCylinder(...)` helpers in `engine/render/utils/VoxelBuilder.ts` for circular silhouettes without increasing voxel counts.
2026-05-06: Converted key high-visibility structures in `engine/data/voxels/buildings/era1/WashPlant.ts`, `engine/data/voxels/buildings/era1/WaterWell.ts`, `engine/data/voxels/buildings/era2/WindTurbine.ts`, `engine/data/voxels/buildings/era2/SocialHub.ts`, and `engine/data/voxels/buildings/era2/SecurityPost.ts` to use rounded or cylindrical forms for a smoother look at similar runtime cost.
2026-05-06: Normalized indexed geometries to non-indexed in `engine/render/utils/VoxelUtils.ts` so rounded primitives can still batch through foliage merge paths without console errors or broken bounding volumes.
2026-05-06: Added zoom-driven building detail tiers in `game/render/systems/BuildingRenderSystem.ts` and `game/AureusWorld.ts`, then taught representative factories like `StaffQuarters`, `SolarArray`, `WashPlant`, and `RecyclingPlant` to render much denser micro-detail when zoomed in and fewer small parts when zoomed out.
2026-05-06: Reworked `engine/render/materials/VoxelMaterials.ts` to use layered mip-smoothed procedural terrain textures for the shared ground master and key surface materials, improving close-up ground richness without introducing external texture assets or extra material variants.
2026-05-06: Tuned rounded voxel thresholds in `engine/render/utils/VoxelBuilder.ts` so more mid-sized building pieces pick up softened corners and slightly fuller radii, keeping tiny micro-detail boxy where extra segment cost is not worth it.
2026-05-06: Upgraded the real runtime renderer path in `game/useAureusEngine.ts` and `engine/render/ThreeRenderAdapter.ts` so the shipped hook now enables antialiasing, fog, ACES tone mapping, and shadow-ready lighting instead of the flatter fallback config.
2026-05-06: Added reusable `dome(...)` and `torusRing(...)` helpers to `engine/render/utils/VoxelBuilder.ts`, with geometry caching and `detailLevel`-aware segment budgets for hybrid smooth/blocky building shapes.
2026-05-06: Converted `engine/data/voxels/buildings/era2/SocialHub.ts`, `MedicalBay.ts`, `CommunityGarden.ts`, `OreExtractor.ts`, and `engine/data/voxels/buildings/era5/GreenTechLab.ts` onto the shared smooth primitive path to prototype higher-visibility non-blocky silhouettes in the live game.
2026-05-06: Verified the prototype by running `npm run build` and loading the real Vite runtime at `127.0.0.1:4173`, capturing `smooth-geometry-runtime-check.png` after the game booted without renderer errors.
2026-05-06: Added adaptive renderer quality selection in `engine/render/ThreeRenderAdapter.ts`, `game/useAureusEngine.ts`, and `game/bootstrap.ts` so constrained devices cap pixel ratio and avoid desktop-grade AA/shadow defaults.
2026-05-06: Added cached building template cloning in `game/render/systems/BuildingRenderSystem.ts` so tile visual rebuilds reuse prebuilt object trees instead of regenerating full building meshes on every dirty/detail refresh.
2026-05-06: Removed per-chunk build logging from `game/render/systems/TerrainRenderSystem.ts` to cut hot-path console overhead during chunk streaming.
2026-05-06: Removed directional stroke overlays from `engine/render/materials/VoxelMaterials.ts` terrain texture generation and replaced them with soft speckle breakup to stop the moving ground-line shimmer introduced by the texture pass.
2026-05-06: Disabled terrain chunk shadow casting in `game/render/systems/TerrainRenderSystem.ts` and softened directional-light shadow bias tuning in `game/render/systems/EnvironmentRenderSystem.ts` to stop large moving self-shadow bands across the ground.
2026-05-06: Reworked `engine/data/voxels/buildings/era1/Road.ts` so corner and angled road decks also solve pitch across both axes, reducing slope-change cracks where direction turns.
2026-05-06: Added a real textured `stone` material and converted `progressGreen` from a flat debug-looking material into a textured green surface in `engine/render/materials/VoxelMaterials.ts`, fixing bright white/green fallback-looking building parts.
2026-05-06: Converted shared `blueMetal` in `engine/render/materials/VoxelMaterials.ts` from a flat color-only material to a textured blue-tinted metal so the remaining teal placeholder-looking building parts render with surface detail.
2026-05-06: Reworked `engine/data/voxels/buildings/era2/Fence.ts` and `engine/data/voxels/buildings/era2/WindTurbine.ts` so large cyan glow slabs became glass-and-metal assemblies with smaller emissive accents, fixing the remaining fence and utility props that still looked like missing textures.
2026-05-06: Reduced the last oversized cyan hero pieces in `engine/data/voxels/buildings/era2/SecurityPost.ts`, `CommunityGarden.ts`, `Generator.ts`, `TrainingCenter.ts`, and `engine/data/voxels/buildings/era1/SolarArray.ts` by wrapping them in glass or shrinking them to smaller powered cores, targeting the final placeholder-looking buildings visible in the world.
2026-05-06: Split terrain and foliage rendering materials in `engine/render/materials/VoxelMaterials.ts`, `game/render/systems/TerrainRenderSystem.ts`, and `FoliageRenderSystem.ts` so biome ground gets biome-aware procedural breakup while foliage keeps its own baked plant colors instead of inheriting the terrain master material and turning into black blobs.
2026-05-06: Fixed follow-up regressions by adding the missing `emissiveOrange` material for `era2/Generator.ts` level 1, shrinking the remaining screen/sign slabs in `era2/Generator.ts`, `era1/StorageDepot.ts`, and `era4/LocalSchool.ts`, and replacing the fragile terrain world-position shader hook in `engine/render/materials/VoxelMaterials.ts` with a safer one after the live ground disappeared.
2026-05-06: Browser console inspection showed the terrain shader was still failing live because `terrainUv` is a `vec2` but the fragment code sampled `terrainUv.z`; fixed that swizzle bug in `engine/render/materials/VoxelMaterials.ts` so the terrain material can compile again.
2026-05-06: Added `engine/render/RuntimeQualityGovernor.ts` and runtime-quality APIs on `engine/render/ThreeRenderAdapter.ts`; the smooth renderer path now samples live FPS plus render timings and steps quality one profile at a time with cooldown/hysteresis instead of relying on a single startup guess.
2026-05-06: Fed runtime quality back into `game/AureusWorld.ts`, `game/render/systems/BuildingRenderSystem.ts`, and `game/render/systems/EnvironmentRenderSystem.ts` so governor downgrades can cap rounded/smooth building detail, reduce pixel ratio, and disable shadow casting in one coherent path without thrashing between states.
2026-05-06: Reworked all prosperity/level-5 landmark factories in `engine/data/voxels/buildings/era5`: `SafariLodge.ts`, `GreenTechLab.ts`, `Monument.ts`, and `Spaceport.ts` now have materially larger silhouettes and much denser architectural detail while keeping the existing footprint contracts intact.
2026-05-06: Verified the prosperity landmark overhaul with `npm run build`.
2026-05-06: Expanded `BuildingType.SAFARI_LODGE` and `BuildingType.SPACEPORT` footprint metadata in `engine/data/buildings.ts` to `7x7`, then rebuilt both voxel factories to actually occupy that extra footprint with resort-campus and launch-facility detail instead of leaving empty margins.
2026-05-06: Caught a malformed `voxel(...)` call in the new `SafariLodge.ts` walkway strip during final inspection, corrected the argument order, and re-verified with `npm run build`.
2026-05-07: Expanded `BuildingType.MONUMENT` in `engine/data/buildings.ts` from `2x2` to `7x7` and rebuilt `engine/data/voxels/buildings/era5/Monument.ts` as a plaza-scale prosperity landmark instead of a compact pedestal prop.
2026-05-07: Verified the monument footprint/detail overhaul with `npm run build`.
2026-05-07: Changed `BuildingType.GREEN_TECH_LAB` from `3x3` to `3x9` in `engine/data/buildings.ts` and rebuilt `engine/data/voxels/buildings/era5/GreenTechLab.ts` as a long-form research campus rather than a square biosphere block.
2026-05-07: Verified the new `3x9` biosphere design with `npm run build`.
2026-05-07: Preserved the `3x9` biosphere contract but added a linked annex in `engine/data/voxels/buildings/era5/GreenTechLab.ts`: `1x1` connector plus a new `3x3` side biosphere block with its own dome and planting detail.
2026-05-07: Verified the biosphere annex addition with `npm run build`.
2026-05-07: Reworked all era-4 sustainability building factories in `engine/data/voxels/buildings/era4` so the full eco tier has materially richer landmark/detail quality: `Reservoir.ts`, `LocalSchool.ts`, `WasteTreatment.ts`, `NatureReserve.ts`, `Hydroponics.ts`, and `GeothermalPlant.ts`.
2026-05-07: Verified the era-4 sustainability building overhaul with `npm run build`.
